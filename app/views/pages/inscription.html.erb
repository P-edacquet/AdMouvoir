<div class="pt-10 lg:pt-28">
  <div class="p-10 lg:p-20 mx-auto max-w-fit border rounded-md border-slate-400">
    <p>
      Veuillez entrer votre email pour que nous puissions garder contact
    </p>

    <%= form_with(url: inscription_create_path, method: :post, data: { turbo_confirm: 'Vérifiez bien votre adresse e-mail, puis cliquez sur OK' }) do |form| %>

      <%= form.hidden_field :post_id, value: params[:post_id] %>

      <div class="my-5">
        <%= form.label :email, class: "text-2xl font-medium text-white" %>
        <%= form.text_field :email, class: "block shadow rounded-md border border-gray-200 outline-none px-3 py-2 mt-2 w-full" %>
      </div>

      <div class="inline">
        <%= form.submit "Valider", class: "btn btn-accent btn-outline" %>
      </div>
    <% end %>
  </div>
</div>