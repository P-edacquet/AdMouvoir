<p>
  Veuillez rentrer votre email pour que nous puissions garder contact
</p>

<%= form_with(url: inscription_create_path, method: :post, data: { turbo_confirm: 'Vérifiez bien votre adresse e-mail, puis cliquez sur ok' }) do |form| %>

  <%= form.hidden_field :post_id, value: params[:post_id] %>

  <div class="my-5">
    <%= form.label :email, class: "text-2xl font-medium" %>
    <%= form.text_field :email, class: "block shadow rounded-md border border-gray-200 outline-none px-3 py-2 mt-2 w-full" %>
  </div>

  <div class="inline">
    <%= form.submit class: "rounded-lg py-3 px-5 bg-blue-600 text-white inline-block font-medium cursor-pointer" %>
  </div>
<% end %>